---
import { Image } from "astro:assets";
import logo from "../assets/minederien.svg";
import Layout from "../layouts/Layout.astro";
---

<Layout
    title="Mine de rien"
    description="Facilitation graphique, illustration, design thinking"
>
    <section class="coming-soon">
        <Image class="logo" src={logo} width={400} alt="Mine de rien" />
        <h1>Mimo se refait une beauté et reviens très vite...</h1>
        <p>
            En attendant, vous pouvez <a
                href="mailto:minederinen.facilitation@gmail.com">m'écrire</a
            > ou me retrouver sur <a
                href="https://www.linkedin.com/in/emilie-allard/">LinkedIn</a>
        </p>
    </section>
</Layout>

<style>
    section {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
    }

    h1,
    p {
        text-align: center;
    }

    h1 {
        font-size: clamp(3.375rem, 3.814vw + 1.64rem, 4.5rem);
        color: var(--color-primary);
    }

    p {
        margin-top: 0;
        font-size: clamp(1.25rem, 1.136vw + 0.5rem, 1.5rem);
    }

    img {
        width: min(80%, 400px);
    }
</style>
